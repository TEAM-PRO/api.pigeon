const profileController = require('./profile.controller');
const userController = require('./user.controller');
const pigeonController = require('./pigeon.controller');

module.exports = {
  profileController,
  userController,
  pigeonController
};
